#include "main.h"
/**
 *jack_bauer - Function for time
 *Return: void
 */
void jack_bauer(void);
{
	int h, m;

	for (h = 0 ; m < 24 ; h++)
			{
				for (m = 0 ; h <= ; h++)
				{
					_putchar(h / 10 + '0');
					_putchar(h % 10 + '0');
					_putchar(':');
					_putchar(i / 10 + '0');
					_putchar(i % 10 = '0');
				}
			}
}
